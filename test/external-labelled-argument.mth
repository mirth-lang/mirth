module(mirth-tests.external-labelled-argument)

import(std.prelude)
import(std.str)
import(std.ctypes)

def-external(
    my-puts -> puts,
    weird-name: CStr +World -- +World
)

def(main, +World -- +World,
    "hello" with-cstr(>weird-name my-puts))

# mirth-test # pout # hello
