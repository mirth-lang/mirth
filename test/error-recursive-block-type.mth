module(mirth-tests.error-recursive-block-type)
import(std.prelude)

def( diverge, *a -- *b,
    [ dup run ] dup run)

def(main, --, id)
# mirth-test # merr # 5:11: error: Failed to unify [*?29 ?30 -- *?32] with ?30
# mirth-test # merr # 5:21: error: Failed to unify [*a ?30 -- *?32] with ?30
# mirth-test # mret # 1
