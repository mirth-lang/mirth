||| Errors should short-circuit the codegen.
module(mirth-tests.error-shortcircuit)
import(std.prelude)
import(std.world)

def(main, --, id)
def(spam, Int, "hello")

# mirth-test # merr # 7:23: error: Failed to unify Int with Str
# mirth-test # mret # 1
