||| Check that hexdigits are escaped correctly in C99 string literals.
module(mirth-tests.hexdigit-escape)
import(std.prelude)
import(std.posix)

def(main, +World -- +World,
    "édead ÁBEEF" print)

# mirth-test # pout # édead ÁBEEF
