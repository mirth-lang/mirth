module(mirth-tests.test-unicode-names)
import(std.prelude)
import(std.posix)

def(Olá-Mundo, +World -- +World, "Hello world!" print)
def(Luís, Int, 1)
def(João, Int, 2)
def(綴, Int Int -- Int, *)
def(🤷‍♀️, +World -- +World, "...whatever" print)

def(main, +World -- +World,
    Olá-Mundo
    Luís show print
    João show print
    10 20 綴 show print
    🤷‍♀️)

# mirth-test # pout # Hello world!
# mirth-test # pout # 1
# mirth-test # pout # 2
# mirth-test # pout # 200
# mirth-test # pout # ...whatever
