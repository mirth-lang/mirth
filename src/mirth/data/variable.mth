module(mirth.data.variable)

table(Variable)
field(variable-name, Variable, Name)
field(variable-type, Variable, Lazy(Type))

def(variable-new!, Name Lazy(Type) -- Variable,
    Variable.alloc!
    tuck variable-type!
    tuck dup2 variable-name!
    DEF_VARIABLE swap name-def!)
