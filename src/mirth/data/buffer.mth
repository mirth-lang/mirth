module(mirth.data.buffer)

# mirth/data/buffer
table(Buffer)
field(buffer-size, Buffer, Size)
field(buffer-name, Buffer, Name)

def(buffer-new!, Name Size -- Buffer,
    Buffer.alloc!
    tuck buffer-size !
    tuck dup2 buffer-name !
    DEF_BUFFER swap name-def !)
