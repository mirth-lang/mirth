module(mirth/data/common)
import(prelude)

# mitrh/data/name
nominal(Name, Int)
nominal(Prim, Name)

# mitrh/data/word
table(Word)

# mirth/data/type
table(MetaVar)
table(TensorType)
table(MorphismType)
table(Nominal)
nominal(Type, U32)

# mirth/data/var
table(Var)

# mirth/data/subst
table(Subst)

# mirth/data/table
table(Table)
table(Field)

# mirth/data/arrow
table(Arrow)
table(Args)
nominal(Op, U8)
nominal(Arg, Arrow)
