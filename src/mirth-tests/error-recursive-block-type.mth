module(mirth-tests.error-recursive-block-type)
import(std.prelude)

def( diverge, *a -- *b,
    [ dup run ] dup run)

def(main, --, id)
# mirth-test # merr # 5:11: error: Failed to unify [*?25 ?26 -- *?28] with ?26
# mirth-test # merr # 5:21: error: Failed to unify [*a ?26 -- *?28] with ?26
# mirth-test # mret # 1
