||| Typechecker should run even when there's no target.
module(tests.error-typechecker)
def(foo, Int, "hello")

# mirth-test # merr # 3:22: error: Failed to unify Int with Str
# mirth-test # mret # 1
