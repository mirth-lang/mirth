||| Typechecker should run even when there's no target.
module(tests.error-typechecker)
def(main, Int, "hello")

# mirth-test # merr # 3:23: error: Failed to unify Int with Str
# mirth-test # mret # 1
