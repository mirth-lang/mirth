module(tests.error-recursive-block-type)
import(prelude)

def( diverge, *a -- *b,
    [ dup run ] dup run)
# mirth-test # merr # 5:11: error: Failed to unify [? ? -- ?] with ?
# mirth-test # merr # 5:21: error: Failed to unify [...] with ?
# mirth-test # mret # 1
